//calcule a área do circulo
// area = pi * raio * raio

const pi = Math.PI
const raio = 10
const area = pi * Math.pow(raio,2)
console.log('A area do circulo foi:',area.toFixed(2))

console.log(`Pi: ${pi.toFixed(2)} * ${raio} *  ${raio} = ${area.toFixed(2)}`)